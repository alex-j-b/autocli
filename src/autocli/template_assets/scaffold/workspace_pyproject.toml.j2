[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "{{ executable_name }}"
version = "0.1.0"
description = "Generated CLI workspace for {{ executable_name }}"
requires-python = ">=3.12"
dependencies = [
{{ runtime_dependencies_block }}
]

[dependency-groups]
dev = [
{{ dev_dependencies_block }}
]

[project.scripts]
{{ executable_name }} = "{{ site_module }}.cli:app"

[tool.setuptools.packages.find]
where = ["."]
include = ["commands*", "shared*", "{{ site_module }}*"]

[tool.autocli]
schema_version = 1
executable_name = "{{ executable_name }}"
site_slug = "{{ site_slug }}"
site_module = "{{ site_module }}"
primary_hosts = [{{ hosts_inline }}]
command_root = "commands"
shared_package = "shared"
